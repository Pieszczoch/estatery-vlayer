import Cards from "@/components/sections/Cards";
import Header from "@/components/sections/Header";

const Home = () => {
  return (
    <main>
      <Header />
      <Cards />
    </main>
  );
};

export default Home;
